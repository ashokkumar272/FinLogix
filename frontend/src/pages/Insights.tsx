import React from 'react'

const Insights = () => {
  return (
    <div className='text-white'>
        hey there
    </div>
  )
}

export default Insights

// Purpose: Detailed spending insights

// Consolidates:

// Previously separate "Charts & Insights" + "AI Advice"

// 📊 Includes:

// Bar chart: Date-wise spending

// Pie chart: Category-wise split

// Line chart (optional): Balance trend

// Time range toggles

// AI Insights section:

// Latest suggestion

// “Generate Insight” button

// Tip history (accordion/list view)

// Visual flags (e.g., red warning if overspending)